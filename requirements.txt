Django>=4.2,<5.3
django-crispy-forms>=2.0
crispy-bootstrap5>=2024.2
Pillow>=10.0
Faker>=20.0
sqlparse>=0.4
tzdata>=2024.1
mysqlclient>=2.2

# Uncomment to use SQLite instead (no extra driver needed):
# just switch the DATABASES block in cabinet_core/settings.py
